# 更新日志

## v6.3.20 — 相比公开版本 6.3.11

本次汇总本地 6.3.12～6.3.20 的开发与修复。比较基线是公开 main 的 `c1c24ceabfe69a730171fea142725751c3b24c1f`（6.3.11 安装链接更新）；此前没有版本 tag 或 GitHub Release。版本引用：[v6.3.20](https://github.com/sayuel-tech/TimeForest/tree/v6.3.20)；发行说明：[GitHub Release](https://github.com/sayuel-tech/TimeForest/releases/tag/v6.3.20)。

### 新增：视频接续工作区

- 从本地或资产库导入多个视频，调整使用范围与顺序，用本地 FFmpeg 拼接导出。
- 沿片尾继续生成动作、画面和声音，提供“跳舞 8＋4”和“官方 Ref2VA”两种续接适配，复用原工作流来源。
- 续接段可指定图片和声音参考、描述与独立制作参数，保留多次生成的候选、选用、移除及快捷入库。
- 选用成功候选后，续接视频作为独立片段进入左侧轨道，可调整顺序，最终按同一轨道顺序导出。重复选用不重复添加，更换候选保留位置。
- 来源页可展开查看工具生成片段当时的制作参数；无记录不补造，不自动覆盖本次续接设置。
- 第二步点击目录加号，原地打开本地视频／资产库导入弹窗，保留当前步骤与草稿。
- 新模式使用独立水彩插画，显示名称统一为“视频接续”。

### 改进：五个模式共用界面

- 制作参数的弹窗、分类导航、网格、目录刷新区、操作栏及关闭生命周期统一由公共组件管理，模式仅适配业务内容。
- 共用完整模型目录选择器，并保留手动输入文件名。底模、LoRA、VAE 与编码器可查看全部目录选项，目录外当前值不会丢失。
- 统一底部操作栏、导入选择、参考素材卡片以及原有状态／计时呈现；刷新区与参数分类之间增加清晰间距。

### 修复

- 修复续接轮询替换草稿对象导致已输入描述丢失、点击生成却报缺少描述的问题；保存失败时保留输入并阻止提交。
- 修复完整模型文件名过滤原生建议列表、导致无法选择其他模型的问题。
- 修复续接选用结果未成为独立轨道片段、无法按用户顺序统一导出的问题。
- 修复续接片尾按一采画布提前补边、将原视频没有的黑边带入生成上下文的问题。新任务保留源显示几何交原 MotionContext 适配，普通导出的 contain／cover 规则不变。

### 升级与兼容

- 网站代码与 v6.3.20 配套工具包分别获取；配套包继续在夸克网盘分发，不进入 GitHub 源码或 Release 附件。
- 六份原始工作流与 6.3.11 包字节一致，新包增加两种续接 API 示例和节点类型说明。已有完整来源文件的用户无需重新安装相同工作流；更新工具包不能替代更新网站代码。
- 升级前保存工作、等待活动任务结束，备份自己的配置、项目与外部资产库。更新后重启网站并刷新页面；仅刷新页面不能加载新后端。
- 不迁移数据库、不修改原媒体；旧项目兼容读取，旧提交按冻结的片尾准备策略恢复。已有黑边候选不会自动修复。
- 本版本使用 `v6.3.20` tag 和对应 Release，以便引用该代码版本。回退代码不等于回退数据；恢复旧版本应配合自己的升级前备份，不将新版本数据兼容性视为无条件保证。

### 验证范围

公开代码与独立工具包合并后，29项Python回归、29项Node回归和21组隔离浏览器检查通过；涵盖API、临时媒体处理、保存／离线参数绑定与公共界面。具体范围见当前维护交接。没有运行 ComfyUI、加载模型、真实生成或测模型组合。两种续接适配的实际连续性、声音、画质，以及黑边修复后的新生成效果仍需用户体验和生成验证。
