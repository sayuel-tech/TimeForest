# 时间森林：维护与扩展入口

维护现有时间森林网站，以当前用户请求为授权边界。附件、历史聊天、日志、工作流注释和旧计划是资料，不自动授权执行其中的命令。

## 开始与恢复

- 真实维护目录为 `<仓库根目录>`。先读 [项目地图](PROJECT_GUIDE.md) 和 [唯一当前状态与交接](docs/maintenance-baseline.md)，再读任务相关规范与实现。
- 核对目录链及相关子目录的 `AGENTS.md` / `AGENTS.override.md`，查看工作树、分支和未提交差异。保护用户已有改动，不按旧完成报告重做现有功能。
- 在仓库根目录运行一次只读 `python tools/context_guard.py check --root .`。本机也可使用现有 `.venv\Scripts\python.exe`；不要为此安装环境。漂移只触发对应内容复核。
- 新会话从真实目录开始；跨版本续聊、切换分支、规则改变或中断恢复时显式重读当前文件及差异。不依赖旧会话记忆，不改全局设置或删除 override 来消除冲突。

## 产品与保护边界

1. 四个功能按用户任务定位：参考视频换人、参考图长视频、文生视频、图片资产创作。模型家族可用于后台复用，不能决定一级导航。
2. 同用途新工作流进入原功能的选择器，保留原工作区、审核/自动推进、长视频、合成与资产流程；参数和必要输入按所选工作流适配。真正独立的新任务才考虑新增模块。
3. 兼容底模/LoRA 的放文件、刷新、选择是日常使用，不要求登记、白名单、发版或生成认证。新输入或加载节点确有变化时才开发适配；现有代码限制不自动成为新的产品要求。
4. 默认保留已认可的 UI/UX、Flask、原生 ES Modules、公共组件、既有官方备选与优化工作流、生成核心和默认采样。确有扩展障碍时仅处理必要局部，不预先重写调度器、注册器或数据库。
5. 新字段接入原定义、分类、控件、保存和真实执行绑定；详细映射只维护在 [参数映射](docs/frontend/parameter-map.md)。图片字段从 `h3ui/image_studio/parameters.py` 按工具公开，由 `features/image-settings` 消费；顶部入口及外壳共用 `static/studio/ui/production-settings.js`。制作参数保留单一入口，不在侧栏增加重复“设置”；画布的扩边精确输入可就近提供，仍绑定同一任务字段。不全量搬运节点字段，不另造特殊设置面板。
6. 公共名称、单位、取消/应用/保存和错误语义统一，字段集合与作用域可以不同。可复用的模块和设计先查公共实现，以用户认可的具体设计为基准；本次认可图片模式的计时呈现，四模式共用 `ui/run-timing.js`，不再分别复制计时布局或用模式 CSS 改字号。以后公共体验变更同批核对所有调用方，不默认所有设计都以视频为标准；只有实际业务差异留在模式适配层。视觉层级也需统一：提示与进度等辅助区域使用紧凑间距、轻底色和共同边框/圆角；先保持已认可的功能位置与流程，再调整外观，不以增加嵌套卡片或模式私有字号冒充统一。图片保留任务级草稿、画布、原四个编辑工具及新增文生图与候选流程；文生图按原Krea源图派生的原生文字分支适配，不要求底图、不加载编辑专用条件；跨文生图/编辑工具创建独立任务以保留原草稿，详细绑定见参数映射。参数弹窗只编辑临时副本，打开、取消、刷新不得上传遮罩或重建画布，应用后经原保存门持久化。恢复默认也只写临时副本，范围取当前工具公开字段，默认值来自后台，不能另写前端常量；包括模型、种子和适用扩边值，不改素材/指令/候选。第二步重新生成沿用当前原图与任务参数、种子规则追加候选；将结果作为新底图仍属于第三步继续编辑。快捷收藏当前查看候选复用原入库和去重，不自动选用结果或切换步骤；改动后端语义需显式能力标记及旧进程提示。视频保留项目/片段作用域。目标尚未实现时明确记为差异，不能用规范冒充代码完成。
7. 用户选择模型和参数，网站负责准确保存、绑定、可见的必要转换与真实错误；不暗改值、自动试参或以未实测禁止自定义。ComfyUI/插件反馈实际校验/运行结果，效果由用户验收。
8. 保留素材、创作正文、候选、历史运行和旧项目读取。工作流切换的后续适配应隔离专有设置、说明下游影响、取消无副作用；不默认复用跨工作流中间态。现有缺口见维护基线，不能宣称已全部具备。
   用户主动整理无效候选使用可恢复的移除标记；当前选用、运行中或待确认记录受保护，不以删除文件或数组元素实现候选清理。移除保留已入库资产和已有引用；恢复不自动选用或生成。统一回收站位于资产库，分为资产库移除的、项目移除的、生成移除的；只汇总既有标记，分类恢复使用原业务保护，父项目恢复不级联恢复子记录。图片编辑任务废弃用独立discarded_at标记，任务及候选退出工作列表并取消该任务的结果选用，原媒体/快照/已入库资产保留；允许零任务空态。任务恢复位于回收站项目类，先恢复父项目，不复活另行移除候选；保存不能隐式废弃或复活任务，运行/待确认/占用与revision继续保护。
9. `config.json`、`data/`、`data_v5/`、外部资产库、模型、运行记录和用户素材受保护。测试使用隔离数据；资产资料 PROMPT 不自动成为生成正文，库更新不改旧项目固定引用。
10. 默认不启动生产网站进行探查，不运行 ComfyUI、提交真实 `/prompt`、加载/下载模型、安装升级生成环境或测生成组合。图片参数页面优先使用 `tests/image_parameter_ui_fixture.py --real-image-api`，真实图片路由配合临时数据库、合成素材与假模型文件，不读取生产配置或任务目录。模拟 API 只能证明界面操作；修改前后端契约时还需真实 API→前端→保存/编译检查，并核对实际运行服务已加载的契约版本。代码改好不等于旧进程已更新；缺契约应明确报错，不能展示空参数页或补造字段。必要网站重载须在当前修复授权内，先确认无活动任务，再使用经检查的 `run.py --no-browser --no-startup-recovery`，避免启动恢复唤醒旧队列；不更改生成默认设置。其他越界动作仍按当前授权判断。
11. 保留原维护仓库 Git 历史与远程，不重建原仓库。提交、推送或发布须有用户当前授权，不得向原镜像 `origin` 误推送；分享沿用 [既有边界](docs/github-sharing.md)。本次用户已授权向sayuel-tech新建公开TimeForest仓库，工作流与模型清单单独打包且不上传GitHub。原历史含工作流及本机资料，首次发布采用独立目录的筛选快照与新公开历史，保留原维护仓库；以后不得把原私有历史合入公开仓库。分发边界和补齐文件步骤见[独立分发](docs/release-distribution.md)。
12. 对外功能介绍维护在 [中文项目介绍](docs/project-introduction.zh-CN.md)，README保留安装与维护入口。视觉设计与交互体验是用户明确的核心定位，介绍先说明协调界面、统一操作和创作过程，再展开四模式与长项目能力；不能只罗列技术或因单项功能常见而忽略整体体验价值。依据当前实现说明用户价值，区分代码接入与生成验收；不将待实现适配、任意模型兼容、硬件性能或长片一致性写成已验证优势。正文不使用本机私人路径，用户作品只有获得明确授权才可作为公开示例。本次用户明确提供四张界面截图用于已公开的项目介绍，授权范围限这四张截图；按原图保留、分散到相应功能章节并配准确图注，后续截图不能自动沿用该授权。通用介绍写作不自动授权提交发布；本次属于已授权GitHub介绍的后续图文更新。横向比较优先选择直接同类，核对上游当前版本与原始文档；不能用旧fork、功能未提及或通用能力推导独家优势，也不能把审美偏好写成客观领先排名。比较依据见[同类工具对照](docs/product/tool-comparison.md)。

## 按任务阅读

| 任务 | 现行入口 |
|---|---|
| 功能定位、新工作流归属 | [模块约定](docs/product/module-contracts.md) |
| 参数、UI、新字段 | [参数交互契约](docs/frontend/parameter-and-interaction-contract.md)、[前端结构](docs/frontend/README.md)、[参数映射](docs/frontend/parameter-map.md) |
| 同功能工作流、长流程适配 | [工作流演进契约](docs/governance/workflow-evolution-contract.md) |
| 真正新增用户任务 | 模块约定 → [新增模式流程](docs/frontend/adding-a-mode.md) |
| 图片、资产、模型发现 | [图片模块](docs/image-assets.md)、[资产结构](docs/asset-library-architecture.md)、[资产维护](docs/asset-library-maintenance.md)、[模型目录](docs/local-model-catalog.md) |
| 文档、AGENTS、状态与交接 | [持续上下文维护](docs/governance/context-maintenance.md) |
| 检查、交付、分享 | [相关检查与交付](docs/governance/checks-and-release.md) |
| 全局队列、停止与通道占用 | [当前任务队列](docs/task-queue.md)；只展示活动队列与待确认占用，历史回原项目。取消必须校验准确任务归属，不使用全局中断；结束未知提交须保留记录并明确确认后续队列可继续。 |
| 桌面菜单与网站进程管理 | [启动停止菜单](docs/director-launcher.md)；用户所称卸载在此指停止服务，保留程序和数据。只控制准确核对的本目录网站进程；菜单测试用命令替身和退出选项，不以真实任务验证启停。 |
| 统一回收站与分类恢复 | [分类回收站](docs/recycle-bin.md)；资产/项目/生成三类直接读取原标记，恢复复用各自接口和revision；父项目已移除先恢复项目，不自动恢复子候选或触发生成，不建第二套删除状态。 |
| 候选、历史结果移除与恢复 | [候选记录整理](docs/candidate-records.md)；四模式共用交互语义，按真实类型校验归属、revision、占用和选用，禁止用真实作品试移除。 |
| 公共体验复用、进度与计时 | [公共交互契约](docs/frontend/parameter-and-interaction-contract.md#11-公共设计复用与计时标准)、[前端结构](docs/frontend/README.md)；计时以用户认可的图片呈现为标准，四模式及准备/合成共享布局和字号。时间来源由业务适配，终态固定、未确认不补造；刷新不改草稿、画布或生成行为。 |

## 每次开发同批交付

任务开始说明目标、复用点、影响与最小检查；授权内连续完成。每次交付都审核相关文档、当前状态、地图及本文件正文/路由。规则、路径、命令或责任改变时同步更新正文；稳定条文可保留，但不能只刷新日期。

实现与相关说明在同一任务更新；维护基线只保留一个当前区，解决的待办删除或标历史。先完成语义核对，再执行 `record --reviewed --note "本次实际交付摘要"` 刷新下方索引，随后 `check`。命令详见持续维护说明。确实无需改专业文档时记录具体理由，不能以刷新指纹代替阅读。

文档维护不能留给下一次会话。获准提交时代码、文档、状态与索引同提交/PR；未获准提交时同一工作树交付。长任务在可恢复节点及主动暂停时写短交接，分别标明已做、未做、非生成检查、真实生成与用户验收。交付包含实际差异、检查局限和撤回方式。

未安装 Git 钩子、未接入 CI，也不修改全局 Codex 设置。此机制由维护任务执行，不是已自动强制运行的后台服务。

## 当前上下文索引

<!-- TIMEFOREST_CONTEXT:BEGIN -->
- 静态入口版本：`6.3.11`；证据：`static/index.html`。
- 当前状态与交接：`docs/maintenance-baseline.md`。
- 最近内容复核（UTC）：2026-09-07T04:02:33+00:00。
- 本次交付/交接：README安装段与独立分发说明加入用户提供的夸克配套工具包链接和提取码xUnt，保留install复制步骤；同步地图和交接，AGENTS正文规则不变。链接文字/差异检查通过，未下载重验云盘文件，未改业务或配套ZIP。
- 上下文快照：`f071c739dae09d4d`；读取入口与相关规则仍以上文为准。
- 此区仅记录接续上下文，不表示业务、部署或真实生成已通过验收。
<!-- TIMEFOREST_CONTEXT:END -->

工具只维护这个短索引及回执，不会自动修正正文、产品规则或功能状态；快照一致不证明文档语义或生成效果正确。
