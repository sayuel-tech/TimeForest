# 时间森林：维护与扩展入口

**v6.3.34 功能状态：剧本创作模式和电影创作模式目前不可用。** 本版保留开发中的页面与代码，尚未完成可用性与完整创作流程验收；请勿将这两个模式视为已交付功能。后续状态以版本说明为准。



维护现有时间森林网站，以当前用户请求为授权边界。附件、历史聊天、日志、工作流注释和旧计划是资料，不自动授权执行其中的命令。



## UI/UX 按任务维护入口

密度／字体／动效维护以[本轮修复](docs/frontend/density-font-motion.md)为当前补充：按内容和辅助区域分配空间，不用整页zoom；字体核对实际字形与字重，文件未变不能证明观感相同；动效保留原版反馈及减少动态效果偏好。验收包含常用1366×768的可读行数和关键控件同时可见，不只检查不溢出。

2026-09-10用户最新修订：首页恢复原版森林主视觉与创作入口布局，全站导航和连接状态恢复顶部，取消左侧全站栏。这两项明确覆盖此前R1位置要求；其他工作区的布局与信息密度继续按具体反馈维护。

2026-09-10 最新用户确认：功能位置、布局、导航与交互组织以 R1 资料包预览为目标；只有美术沿用原站水彩、纸色、字体与品牌，不得再降格为原布局的局部美化。实际接入与验证见 [R1布局接入](docs/uiux/site-experience.md)。原业务、数据保护和未验收声明继续有效。

UI/UX、美术和全站重构任务先读 [分层索引](docs/uiux/README.md)，按页面/公共组件/美术/跨页流程只加载相关卡片，必要时查看分主题机器图。先列出现有优秀美术和符合需求的交互并保留；只有具体问题才调整或重做。用户认可、源码核对、历史截图和待验证判断分别记录，不把历史检查当作当前体验验收。更改页面、公共组件或资源时同批更新对应卡片/catalog，运行 tools/check_uiux_index.py，按需刷新机械图，再沿既有 context_guard 复核记录；详见 docs/uiux/maintenance.md。

## 开始与恢复



最新剧本方案约束：没有“段落”业务层；图片/故事→完整剧本→LLM资产需求分析与命名卡片绑定→完整资产剧本→分镜→分镜下片段→Prompt。左侧是跨页目录，分镜到第四页、片段到第五页；第三页保留左侧但无右侧栏，主区上资产下剧本。第二/四/五页统一上沟通下输出，右侧为当前参考/有效资产。分镜可沿用全剧或独立设定；片段可沿用分镜、直接全剧或独立设定；逐项显示真实来源和局部覆盖。本轮按用户“开始执行”已接入，实际交付及未验收范围见 docs/frontend/creation-experience-implementation.md；后续仍按真实作用域和实际输入检查，不以inherit字段或CSS相同视作体验已获用户认可。



剧本/电影当前须优先读[交互流程纠偏方案](docs/product/creation-experience-correction.md)：用户已否定当前业务流程完成度，旧E0—E6与UI检查只保留为工程记录。用户已后续授权实施，本轮已交付沟通/输出/确认、命名卡片、跨页目录及来源解析。后续维护继续以沟通→输出→确认→纵向目录及后续资产/分镜/Prompt/电影闭环为标准，不再以表单/API存在或CSS统一代替功能完成。写作新任务必须接入按内容对象隔离的conversation草稿、同一输出编辑/确认机制、真实有效资产解析和原保存/冲突门；规则与入口见 docs/frontend/creation-experience-implementation.md。



- 真实维护目录为 `<仓库根目录>`。先读 [项目地图](PROJECT_GUIDE.md) 和 [唯一当前状态与交接](docs/maintenance-baseline.md)，再读任务相关规范与实现。

- 核对目录链及相关子目录的 `AGENTS.md` / `AGENTS.override.md`，查看工作树、分支和未提交差异。保护用户已有改动，不按旧完成报告重做现有功能。

- 在仓库根目录运行一次只读 `python tools/context_guard.py check --root .`。本机也可使用现有 `.venv\Scripts\python.exe`；不要为此安装环境。漂移只触发对应内容复核。

- 新会话从真实目录开始；跨版本续聊、切换分支、规则改变或中断恢复时显式重读当前文件及差异。不依赖旧会话记忆，不改全局设置或删除 override 来消除冲突。



## 产品与保护边界



1. 当前五个功能按用户任务定位：参考视频换人、参考图长视频、文生视频、图片资产创作、视频接续。模型家族不能决定一级导航。视频接续的普通拼接走本地媒体通道，跳舞8＋4与官方来源适配仅作用于AI续接；外部声画尾部不能冒充原内部二采无损来源，保留原片及旧模式默认语义。当前接入和未生成验收边界见[视频接续](docs/video-assembly.md)。用户已于2026-09-09授权v6.3.32代码与配套工具包一并更新GitHub；源码与tag同批推送，工作流和模型名称ZIP作为同版Release附件，安装说明指向它，不再等待夸克地址。普通开发不自动授权下一次发布。

2. 同用途新工作流进入原功能的选择器，保留原工作区、审核/自动推进、长视频、合成与资产流程；参数和必要输入按所选工作流适配。真正独立的新任务才考虑新增模块。

3. 兼容底模/LoRA 的放文件、刷新、选择是日常使用，不要求登记、白名单、发版或生成认证。新输入或加载节点确有变化时才开发适配；现有代码限制不自动成为新的产品要求。

4. 默认保留已认可的 UI/UX、Flask、原生 ES Modules、公共组件、既有官方备选与优化工作流、生成核心和默认采样。确有扩展障碍时仅处理必要局部，不预先重写调度器、注册器或数据库。

5. 新字段接入原定义、分类、控件、保存和真实执行绑定；详细映射只维护在 [参数映射](docs/frontend/parameter-map.md)。图片字段从 `h3ui/image_studio/parameters.py` 按工具公开，由 `features/image-settings` 消费；顶部入口及外壳共用 `static/studio/ui/production-settings.js`。制作参数保留单一入口，不在侧栏增加重复“设置”；画布的扩边精确输入可就近提供，仍绑定同一任务字段。不全量搬运节点字段，不另造特殊设置面板。

6. 公共名称、单位、取消/应用/保存和错误语义统一，字段集合与作用域可以不同。可复用的模块和设计先查公共实现，以用户认可的具体设计为基准；本次认可图片模式的计时呈现，五模式共用 `ui/run-timing.js`，不再分别复制计时布局或用模式 CSS 改字号。以后公共体验变更同批核对所有调用方，不默认所有设计都以视频为标准；只有实际业务差异留在模式适配层。视觉层级也需统一：提示与进度等辅助区域使用紧凑间距、轻底色和共同边框/圆角；先保持已认可的功能位置与流程，再调整外观，不以增加嵌套卡片或模式私有字号冒充统一。图片保留任务级草稿、画布、原四个编辑工具及新增文生图与候选流程；文生图按原Krea源图派生的原生文字分支适配，不要求底图、不加载编辑专用条件；跨文生图/编辑工具创建独立任务以保留原草稿，详细绑定见参数映射。参数弹窗只编辑临时副本，打开、取消、刷新不得上传遮罩或重建画布，应用后经原保存门持久化。恢复默认也只写临时副本，范围取当前工具公开字段，默认值来自后台，不能另写前端常量；包括模型、种子和适用扩边值，不改素材/指令/候选。第二步重新生成沿用当前原图与任务参数、种子规则追加候选；将结果作为新底图仍属于第三步继续编辑。快捷收藏当前查看候选复用原入库和去重，不自动选用结果或切换步骤；改动后端语义需显式能力标记及旧进程提示。视频保留项目/片段作用域。目标尚未实现时明确记为差异，不能用规范冒充代码完成。

7. 用户选择模型和参数，网站负责准确保存、绑定、可见的必要转换与真实错误；不暗改值、自动试参或以未实测禁止自定义。ComfyUI/插件反馈实际校验/运行结果，效果由用户验收。

8. 保留素材、创作正文、候选、历史运行和旧项目读取。工作流切换的后续适配应隔离专有设置、说明下游影响、取消无副作用；不默认复用跨工作流中间态。现有缺口见维护基线，不能宣称已全部具备。

   用户主动整理无效候选使用可恢复的移除标记；当前选用、运行中或待确认记录受保护，不以删除文件或数组元素实现候选清理。移除保留已入库资产和已有引用；恢复不自动选用或生成。统一回收站位于资产库，分为资产库移除的、项目移除的、生成移除的；只汇总既有标记，分类恢复使用原业务保护，父项目恢复不级联恢复子记录。图片编辑任务废弃用独立discarded_at标记，任务及候选退出工作列表并取消该任务的结果选用，原媒体/快照/已入库资产保留；允许零任务空态。任务恢复位于回收站项目类，先恢复父项目，不复活另行移除候选；保存不能隐式废弃或复活任务，运行/待确认/占用与revision继续保护。

9. `config.json`、`data/`、`data_v5/`、外部资产库、模型、运行记录和用户素材受保护。测试使用隔离数据；资产资料 PROMPT 不自动成为生成正文，库更新不改旧项目固定引用。

10. 默认不启动生产网站进行探查，不运行 ComfyUI、提交真实 `/prompt`、加载/下载模型、安装升级生成环境或测生成组合。图片参数页面优先使用 `tests/image_parameter_ui_fixture.py --real-image-api`，真实图片路由配合临时数据库、合成素材与假模型文件，不读取生产配置或任务目录。模拟 API 只能证明界面操作；修改前后端契约时还需真实 API→前端→保存/编译检查，并核对实际运行服务已加载的契约版本。代码改好不等于旧进程已更新；缺契约应明确报错，不能展示空参数页或补造字段。必要网站重载须在当前修复授权内，先确认无活动任务，再使用经检查的 `run.py --no-browser --no-startup-recovery`，避免启动恢复唤醒旧队列；不更改生成默认设置。其他越界动作仍按当前授权判断。

11. 保留原维护仓库 Git 历史与远程，不重建原仓库。提交、推送或发布须有用户当前授权，不得向原镜像 `origin` 误推送；分享沿用 [既有边界](docs/github-sharing.md)。公开TimeForest仓库已建立；本次用户授权更新sayuel-tech/TimeForest并创建v6.3.32 tag与GitHub Release，工作流与模型清单ZIP上传为Release附件，不混入源码Git树。原历史含工作流及本机资料，首次发布采用独立目录的筛选快照与新公开历史，保留原维护仓库；以后不得把原私有历史合入公开仓库。分发边界和补齐文件步骤见[独立分发](docs/release-distribution.md)。

12. 对外功能介绍维护在 [中文项目介绍](docs/project-introduction.zh-CN.md)，README保留安装与维护入口。视觉设计与交互体验是用户明确的核心定位，介绍先说明协调界面、统一操作和创作过程，再展开五模式与长项目能力；不能只罗列技术或因单项功能常见而忽略整体体验价值。依据当前实现说明用户价值，区分代码接入与生成验收；不将待实现适配、任意模型兼容、硬件性能或长片一致性写成已验证优势。正文不使用本机私人路径，用户作品只有获得明确授权才可作为公开示例。本次用户明确提供四张界面截图用于已公开的项目介绍，授权范围限这四张截图；按原图保留、分散到相应功能章节并配准确图注，后续截图不能自动沿用该授权。通用介绍写作不自动授权提交发布；本次属于已授权GitHub介绍的后续图文更新。横向比较优先选择直接同类，核对上游当前版本与原始文档；不能用旧fork、功能未提及或通用能力推导独家优势，也不能把审美偏好写成客观领先排名。比较依据见[同类工具对照](docs/product/tool-comparison.md)。



用户明确要求跨模式操作体验一致：功能内容可以不同，公共动作的含义、位置、反馈与草稿保护应可预测。验收从相同用户操作出发，不能把共同CSS/函数引用当作体验一致的证据，也不能用业务差异解释同名保存/取消行为不同。完善目标与分批验收见[体验一致性方案](docs/product/experience-consistency-plan.md)；首批参数/保存与离开保护已在本地6.3.22落地，见[首批交付](docs/frontend/experience-phase1.md)；第二批导航与底栏见[第二批交付](docs/frontend/experience-phase2.md)，第三批候选/播放见[第三批交付](docs/frontend/experience-phase3.md)；第四批异步接收、传输阶段、任务反馈与持久入库回执见[第四批交付](docs/frontend/experience-phase4.md)；四批代码落地不代表真实生成和日常体验已全量验收，不把部分落地写成全站体验已经一致。



公共UI维护须覆盖完整外壳与样式归属，不能仅因复用HTML片段就宣称统一。模式只配置字段、插槽、能力与业务回调；公共容器、网格、操作栏、响应式和关闭生命周期由公共入口负责，不依赖模式自行填写隐式ID或私有CSS补样式。修改共同呈现同批核对所有调用方。用户已授权按[公共界面优化方案](docs/product/shared-ui-optimization-plan.md)实施；本地6.3.13已接入完整参数外壳、底栏分组、导入区和续接参考链。后续维护先读[公共组件归属](docs/frontend/shared-ui.md)，使用openProductionSettings、workspaceActions和referenceAssetCard等入口；保持业务适配，不复制公共CSS。真实生成待验；发布按配套包与地址先就绪、代码与tag同批交付的顺序执行。



模式名称与入口插画统一由 `app/mode-registry.js` 注册；视频接续沿用 `video_assembly` 内部标识。每个独立模式使用对应主题的独立插画，复用纸色、水彩与森林视觉语言，不复用另一模式的同一图片。资源来源和提示词记入[美术资源](docs/image-assets-art.md)；改显示名不批量重命名用户项目。



状态轮询接收不得替换仍由输入事件引用的草稿对象而不重绘；无变化保持对象，有变化时接收和重新绑定同步。请求发出后也可能开始编辑，接收时复核草稿、输入焦点、操作及弹窗；播放暂缓重绘时同样暂缓替换。保存失败保留输入并停止生成提交。相关回归须覆盖等待轮询后再输入及迟到响应，入口见[公共组件归属](docs/frontend/shared-ui.md)和tests/video_assembly_draft_ui_fixture.py；不把仅快速填写通过当作草稿可靠性验收。



视频接续左侧目录是成片轨道：原视频和选用的成功续接独立显示、独立排序，导出必须消费相同顺序。track_order与生成依赖/运行快照分离，排序不重新生成、不伪造来源、不复制媒体来冒充轨道项；重复选用幂等、更换候选保持槽位，旧项目只读投影。移除与恢复仍用原业务保护；assembly_track_version=1缺失时提示重启，不默默丢弃排序。详见[视频轨道](docs/video-assembly.md#视频轨道与导出顺序)。



来源参数展示必须来自所选媒体固定版本的制作时记录，不能用原项目当前设置、根资产其他媒体或默认值补齐。来源查看只读且不自动应用；无记录保持空白，复合成片按实际片段/内部任务区分。视频接续source_parameters是展示字段，不能放进provenance改变旧候选的来源签名，也不能接受客户端保存覆盖；详见[原片段制作参数](docs/video-assembly.md#原片段制作参数)。



公共模型文件选择统一走 `ui/model-selector.js`（由production-settings调用），完整目录下拉与折叠手动输入共存；不能用当前完整文件名过滤的datalist代替模型选择。公共目录刷新区的容器和间距归productionSettingsMarkup及production-settings.css维护。改动须同批核对五模式，并做真实隔离目录→选择→保存→离线编译绑定；不以目录可列出当作模型生成认证。



视频接续非第一步的视频目录加号应原地打开添加视频弹窗，提供本地视频与资产库两个同级来源，不借添加操作把用户送回第一步。复用公共scopedModal/importOptions与原保存/导入链；取消不保存，导入前保存失败必须停止，成功追加并选中新片段而不改变步骤。



AI续接片尾预处理与成片导出尺寸适配须分开：片尾保留源显示几何交给原MotionContext节点，不用导出contain规则预先补黑边；不能通过播放器裁切掩盖媒体问题。tail_preparation按运行快照固定，旧运行缺字段保留旧准备语义；原片已有边框和已生成结果不自动裁除。详情及回归见[接续时长与声音](docs/video-assembly.md#时长与声音)和tests/test_video_assembly_tail_geometry.py。



资产覆盖维护先核对实际模式适配及[资产系统审查](docs/asset-system-audit-20260908.md)：共用Library或选择器不代表引用登记、最近使用、绑定与目标转入全覆盖。改动同批核对五模式各自保存成功/取消、固定副本、幂等入库及库侧使用记录。新资产使用在项目提交后走 `Library.complete_usage` / `usage.py`，不在选择器或图片输入准备时登记；refs 是历史使用，used 不是每次生成时间。跨库失败保留补登记凭据，重试不重复计时，不自动重写历史。图片转入按 `asset-picker/destinations.js` 区分分镜与接续续写段；整改与局限见[本轮整改](docs/asset-system-improvements-20260908.md)。



## 按任务阅读



剧本／电影模式已于2026-09-09获用户V3.0实施授权，当前进度以[实施账本](docs/product/script-film-execution.md)为准；旧[研究建议](docs/product/script-film-implementation-design.md)仅作映射资料。五页剧本、两页电影与双轴固定；新业务使用原项目/资产/Prompt/任务系统，电影采用不复用旧下游清选。DeepSeek Chat初始可编辑模型为deepseek-v4.1-flash-expires-on-0910；本地LLM只留合同和配置，不调用。测试仅临时数据和fake模型/媒体末端，禁止读取生产密钥、真实推理、Comfy提交、生产重载及发布。新插画必须符合原站水彩，未单独授权不得调用图像模型；未完成美术不能宣称整体完成。



| 任务 | 现行入口 |

|---|---|

| 功能定位、新工作流归属 | [模块约定](docs/product/module-contracts.md) |

| 参数、UI、新字段 | [参数交互契约](docs/frontend/parameter-and-interaction-contract.md)、[前端结构](docs/frontend/README.md)、[参数映射](docs/frontend/parameter-map.md) |

| 同功能工作流、长流程适配 | [工作流演进契约](docs/governance/workflow-evolution-contract.md) |

| 真正新增用户任务 | 模块约定 → [新增模式流程](docs/frontend/adding-a-mode.md) |

| 视频接续与AI尾部续接 | [现行使用与维护](docs/video-assembly.md)、[原开发计划](docs/product/video-assembly-plan.md)；后端 `h3ui/video_assembly/` 与前端 `modes/video-assembly/`，普通拼接/生成分通道，恢复原提交不重复发送。 |

| 图片、资产、模型发现 | [图片模块](docs/image-assets.md)、[资产结构](docs/asset-library-architecture.md)、[资产维护](docs/asset-library-maintenance.md)、[模型目录](docs/local-model-catalog.md) |

| 文档、AGENTS、状态与交接 | [持续上下文维护](docs/governance/context-maintenance.md) |

| 检查、交付、分享 | [相关检查与交付](docs/governance/checks-and-release.md) |

| 全局队列、停止与通道占用 | [当前任务队列](docs/task-queue.md)；只展示活动队列与待确认占用，历史回原项目。取消必须校验准确任务归属，不使用全局中断；结束未知提交须保留记录并明确确认后续队列可继续。 |

| 桌面菜单与网站进程管理 | [启动停止菜单](docs/director-launcher.md)；用户所称卸载在此指停止服务，保留程序和数据。只控制准确核对的本目录网站进程；菜单测试用命令替身和退出选项，不以真实任务验证启停。 |

| 统一回收站与分类恢复 | [分类回收站](docs/recycle-bin.md)；资产/项目/生成三类直接读取原标记，恢复复用各自接口和revision；父项目已移除先恢复项目，不自动恢复子候选或触发生成，不建第二套删除状态。 |

| 候选、历史结果移除与恢复 | [候选记录整理](docs/candidate-records.md)；五模式共用交互语义，按真实类型校验归属、revision、占用和选用，禁止用真实作品试移除。 |

| 公共体验复用、进度与计时 | [公共交互契约](docs/frontend/parameter-and-interaction-contract.md#11-公共设计复用与计时标准)、[前端结构](docs/frontend/README.md)；计时以用户认可的图片呈现为标准，五模式及准备/合成共享布局和字号。时间来源由业务适配，终态固定、未确认不补造；刷新不改草稿、画布或生成行为。 |

| UI、UX及代码复用分析 | [6.3.21复用审查](docs/ui-code-reuse-audit-20260908.md)区分已共用、覆盖缺口和业务差异；该历史报告是修复前证据，最新实施范围见体验首批至第四批交付与维护基线。既有公共组件位置仍见shared-ui.md。 |



## 新功能的共同体验准入



新增模式、页面、字段、工作流和公共样式均须遵循[体验准入](docs/frontend/experience-admission.md)。开工先列公共入口、作用域、业务差异与验收场景；已有公共框架优先补能力，不另写模式私有副本。按该文档模板记录相同操作的实际结果，公共改动核对全部受影响调用方。普通新功能同样适用，不能因未新增模式绕过评审。



五模式参数操作通过openProductionSettings返回的run/showError/cancel共用防重与失败留存；参数保存和离开保存调用各自原保存门。需要确认保存影响时用ui/choice-dialog.js独立实例，不替换底层参数弹窗。共同草稿状态由ui/draft-status.js提供；保存失败保留编辑，正在保存时不允许关闭来伪装撤销。



五模式项目页头和步骤必须经ui/workspace-chrome.js；底栏经ui/workspace-actions.js及workbench.css。返回/保存属于左支持区，下一步/主动作属于右动作区；模式只适配标题、状态、步骤和业务命令，不复制模板或写模式专属的底栏响应式覆盖。步骤键盘只移动焦点，点击仍走原保存/准备门，不在公共模板里触发生成。新增功能同样遵循；实际边界与检查见[第二批交付](docs/frontend/experience-phase2.md)。



结果操作栏与候选状态共用ui/result-view.js，视频播放共用ui/media-player.js，移除/恢复共用ui/candidate-records.js；样式归workbench.css，图片画布、原视频历史详情与接续轨道仍按业务配置。查看不暗中选用，入库不暗中选用或跳步骤；选用还会合成/继续制作时写明后果。原三视频重新生成会取消本段选用并使依赖结果过期，须确认影响，不借UI统一修改原执行语义。已入库标记必须来自真实成功回执：图片/接续读持久字段，前三视频从outputs接口的result_receipts_version=1读取既有完成入库记录，工作区缓存仅用于呈现；缺契约提示重启，不猜测或伪造。播放动作定位公共播放器，不误操作轨道缩略视频；恢复不自动选用或生成。新功能必须声明results/playback复用或业务不适用，详见第三批交付。



异步共同机制归core/async-state.js、core/progress-channel.js、core/upload-client.js及ui/async-feedback.js；任务事实归core/task-state.js。模式传对象/编辑代次和真实请求，接收时再次核对归属、版本、输入焦点、弹窗与播放状态。暂缓重绘时保留草稿对象，允许仅更新运行事实；旧请求不能覆盖新保存对象或新弹窗。切页中断界面读取，不发送引擎停止；请求结果不明先核对状态，不自动重试写入。文件传输完成与服务器处理完成分开，完整原始错误保留。资产入库回执从既有operations及实际资产读取，不能凭文件名、当前选用或前端缓存推断。新增调用方声明async/transfers/taskStates适配，回归包括迟到响应、双击、关闭重开和断线恢复。



每次前端开发交付运行 `node tools/check_experience_contract.mjs`；修改检查器时另运行 `node --test tests/experience_contract.test.mjs`。新注册模式须加入 `docs/frontend/experience-contract.json` 的 modes 并附接入与场景证据，不能扩大历史 baseline 豁免。检查只验证登记结构与文件存在，不证明复用调用、操作一致或美观；实际检查失败/未验不能标为完成。既有五模式缺口继续按方案逐批修复，不能用新规范或结构通过冒充整改完成。



资产共同体验维护见[6.3.26交付](docs/asset-experience-20260908.md)：来源项目、上游资产、历史使用项目分别展示，不从refs推断生成归属。来源解析归asset_library/origins.py与generation_records.py，展示归ui/asset-origin.js与source-parameters.js；各模式只适配确切身份与目标用途。固定媒体参数不读当前项目或父资产值补齐。选择器预览不选用，选择锁定列表版本。新功能声明assetSelection/assetOrigins/assetUsage/assetCollection及assetLifecycle验收；历史缺口不补造，准入结构通过不等于体验认证。





来源项目内的记录定位统一用core/source-target.js和ui/source-navigation.js；模式只配置查看位置，不把来源跳转当作选用、保存或生成。确切候选/任务/导出身份来自固定媒体来源，缺失不猜最新，移除给真实回收路由。图片跨任务查看不得写current_task；原视频候选在历史栏展开。新增资产来源适配必须检查定位和无副作用，见[6.3.27交付](docs/asset-source-navigation-20260908.md)。





添加素材时，打开选择器/用途对话框及取消不得提前保存项目或上传标注。原三视频库引用走asset_import_draft_version=1和ProjectImport的draft预览，确认后与引用一起提交；不自行先ensureSaved冒充取消无副作用。用途字段/校验/临时编辑与取消生命周期复用ui/reference-metadata.js，业务只适配目标和提交。图片A/B移除是任务引用草稿操作，保留原件/库/候选；A标注清空须明确说明，B不清A。三阶段资产总进度及检查范围见[6.3.28交付](docs/asset-lifecycle-20260908.md)，后续交付持续更新阶段状态，不只报局部通过。



派生追溯统一走asset_library/lineage.py，已入库下游走descendants.py按需分页。上游按执行快照冻结身份，图片以有效inputs来源为准，不能因任务残留parent_output误认父图；接续不读当前选用或track_order补历史。父资产严格核对版本/媒体/摘要；外部包ID不自动映射本机。来源与下游共用资产详情/选择器组件，异常、循环、展开限额及查询范围必须明确。新增功能要记录可核对父身份并提供实际准入证据；第三阶段当前范围与剩余工作见[6.3.29交付](docs/asset-lineage-20260909.md)。



前三视频生成入口通过generation/source_lineage.py旁路冻结输入与父候选身份；外层分镜和内部任务分别归属，不修改previous、提示词、图或随机数。资产来源下游分已入库与项目生成结果两类，共用descendants-view，后者由generation_descendants只读分页，不能以登记operations或重新入库来查询。新结果类型同批接入来源/定位/两类下游；旧缺失元数据不补造，外部包仍不自动映射本机。最新进度与证据见[6.3.30交付](docs/asset-runtime-lineage-20260909.md)。



素材包跨库来源统一归asset_library/pack_lineage.py，包格式2只将清单中明确包含、固定asset/version/media/hash匹配的关系转换为接收库身份；不是按同名、同哈希或外部项目编号自动找对应对象。导出不自动捎带未选上游媒体；工作流资料与来源关系独立选择。导入先验证来源与绑定的联合依赖，再以固定版本回执恢复；重试不能改用后来版本。旧包逐媒体保持外部身份隔离，不能绕过根portable_pack边界。新资产类型同时检查包内往返、外部缺失、重复导入/中断恢复、同编号项目隔离、参数只读与旧后台能力提示。实现与边界见[6.3.31交付](docs/asset-pack-lineage-20260909.md)。



## 统一提示词库维护路由



统一提示词管理的本期范围、未来剧本/本地及云端LLM边界和唯一阶段账本见[执行方案](docs/product/prompt-library-execution-plan.md)。方案V3按用户要求采用视频/图片/剧本→模型家族→提示词编辑，分类含增改排序、移动、另存与可恢复移除；家族须核对实际工作流主模型及作者上游来源，不能从模式名、权重变体或辅助编码器猜测。当前配方的H3/Krea2来源证据见方案4.3，新增模型按同法核实；未知分类不阻止原模型使用和项目保存。用户已确认保存项目成功后自动收录并分类，读取同次已保存的实际模型/正文上下文；重复保存幂等，库失败与项目保存失败分开，手工模板不被覆盖，通用是明确的跨模型整理分支。剧本普通文本管理不依赖LLM接入，分类选择不改生成模型。本地6.3.32已完成P1～P5本期范围，实际入口、配置、检查和边界见[提示词库](docs/prompt-library.md)，不得再按旧规划重做。公共库管理文本、固定版本与来源，五模式只适配作用范围和原保存门；应用只改草稿，收藏不等于保存项目，资产资料不自动成为正文。剧本作品/会话/LLM服务与库分开，由V3.0的creation业务层承载，不挤入提示词库；不将模型凭据写入可分享提示词。每次交付同批更新账本和现行说明，按用户要求只做相关必要检查。



提示词库后端归h3ui/prompt_library，库与项目保存分离；只在原保存成功后capture固定快照，资产引用联合提交与独立草稿保存同样适用。库失败单独补记，禁止重存项目或读取此刻草稿冒充旧快照。family/evidence/model来自实际主模型已核实来源；未知保持待归类，不能阻止使用。prompt_sources仅保存可选固定库版本/制作记录及原文，原编译器不据此生成指令。公共呈现归ui/prompt-editor.js，操作协调和业务适配归features/prompt-library；UI层不倒依赖业务feature。五模式同批核对取消、应用、实际保存、收录与固定记录。新增生成模式须登记promptEditing/promptCollection/promptRecords及prompts场景；分类管理、正文版本、保存事实与生成事实不能混为同一种数据。备份提示词库与资产备份分别说明，新数据库和补记回执不得进入源码或配套包。



## 检查范围与交付完成



用户已明确测试边界（2026-09-09）：以本次改动直接涉及的必要检查作为交付标准，通过后停止扩大或重复测试。真实生成、大文件断网续传、大型资产库、长时间多窗口使用及整体视觉/操作感由用户实际使用反馈，不主动搭建复杂压力或长时测试，也不将这些未测试场景列为阻碍计划完成的开发待办。出现具体反馈、已知失败或改动直接涉及相应风险时，再做针对性复现与检查。保留必要的数据保护、真实保存/参数绑定及受影响公共调用方检查；不把未测试说成已验证。



## 每次开发同批交付



任务开始说明目标、复用点、影响与最小检查；授权内连续完成。每次交付都审核相关文档、当前状态、地图及本文件正文/路由。规则、路径、命令或责任改变时同步更新正文；稳定条文可保留，但不能只刷新日期。



实现与相关说明在同一任务更新；维护基线只保留一个当前区，解决的待办删除或标历史。先完成语义核对，再执行 `record --reviewed --note "本次实际交付摘要"` 刷新下方索引，随后 `check`。命令详见持续维护说明。确实无需改专业文档时记录具体理由，不能以刷新指纹代替阅读。



文档维护不能留给下一次会话。获准提交时代码、文档、状态与索引同提交/PR；未获准提交时同一工作树交付。长任务在可恢复节点及主动暂停时写短交接，分别标明已做、未做、非生成检查、真实生成与用户验收。交付包含实际差异、检查局限和撤回方式。



未安装 Git 钩子、未接入 CI，也不修改全局 Codex 设置。此机制由维护任务执行，不是已自动强制运行的后台服务。



## 当前上下文索引



<!-- TIMEFOREST_CONTEXT:BEGIN -->
- 静态入口版本：`6.3.34`；证据：`static/index.html`。
- 当前状态与交接：`docs/maintenance-baseline.md`。
- 最近内容复核（UTC）：2026-09-10T04:32:13+00:00。
- 本次交付/交接：Reviewed sanitized v6.3.34 public baseline; compatible v6.3.33 companion retained.
- 上下文快照：`0ff7f3f620b95b67`；读取入口与相关规则仍以上文为准。
- 此区仅记录接续上下文，不表示业务、部署或真实生成已通过验收。
<!-- TIMEFOREST_CONTEXT:END -->



工具只维护这个短索引及回执，不会自动修正正文、产品规则或功能状态；快照一致不证明文档语义或生成效果正确。





V3.0 的实际 API/配置与验证边界见 [现行说明](docs/creation-modes.md)。固定来源与准备快照不能用当前草稿回填；采用不重写下游或剪辑，来源返回必须兼容原项目 UUID 与新固定目标 ID。原回收/usage/来源/Prompt 库统一接入，恢复不选用。本期5张插画已在用户单独授权后制作并接入核对（docs/evidence/creation-v3/art）；以后出现 art_pending 仍须在体验登记中保留为 pending（检查失败），不能加历史豁免，只在成品接入并核对后关闭。修改 creation/resources 要同步运行路由/profile/模板说明与静态配套；发行号不因方案号自动升级。





后台读取不等于重建页面。七模式共用core/snapshot-update和contracts/project-refresh区分无变化、运行事实与内容变化；进度经ui/status-region局部更新，不能将其用于带草稿闭包的编辑器。内容替换仍须通过async-state保护并由workspace-view-state保存展开/关闭、滚动、焦点和同媒体节点；暂缓内容不能因合入运行事实而被吞掉。按稳定条目身份隔离，验证无变化、仅进度、内容变化和输入期间响应。网页隐藏仅暂停读取，不停止任务；不能关掉任务同步掩盖刷新问题。新模式必须声明refresh/viewState/statusRegions/libraryNavigation准入，普通页面同样遵循[全站更新规范](docs/frontend/refresh-and-libraries.md)。



资产/提示词管理共用ui/library-navigation与styles/collection-navigation的完整导航和页头；用途/模型保持父子层级、完整当前位置与选中反馈。全库查找不能同时误标某个分支；选择分类退出全库范围。资产缩略区固定紧凑高度并自适应列数，不随宽屏无限放大；原图仅在预览/详情显示。“更多导入方式”使用scopedModal与importOptions，不在页头直接展开挤压；取消保留筛选、多选与原数据。相关调用方与检查见同一规范。





全站设计以[任务与可阅读性规范V2](docs/frontend/visual-design-standard-v2.md)及[页面任务清单](docs/frontend/page-purpose-design.md)为准，V1尺寸规范已被用户退回。先确定页面任务、主要阅读/比较内容、高频操作和辅助信息，再选公共框架；不能把统一理解为统一三栏、缩小正文或固定空白高度。新页面/步骤/重要弹窗必须填写任务卡；designScale登记和检查器只证明结构，不代表阅读体验。用有差异、接近实际长度的临时内容完成主任务、全文阅读、切换与返回，保留原草稿/保存/轮询机制。R1—R4本期改造已实施，实际入口、调用方与检查见[任务布局交付](docs/frontend/readability-implementation.md)。提示词用同会话列表/阅读视图；来源长文用ui/prompt-editor的readingDisclosure/openReading；属性开关由workbench维护，只改变布局、不重建输入。task-layouts.css负责跨模式的任务层组合，新增页面先读任务卡并核对长内容。不能沿用V1通过数量代替此次阅读证据，也不能把隔离通过当作用户审美已确认。数值继续由design-tokens.css和公共组件维护，按任务增加阅读角色，不在模式私有CSS复制。
