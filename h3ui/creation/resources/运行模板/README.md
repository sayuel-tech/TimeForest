# 内部运行模板

本目录纯TXT由网站读取，组合公共约定＋当前任务＋工作流能力＋对应Schema＋获准资料。不要发送Markdown设计说明标题，也不要整目录一次发给模型。

任务和正文取值见../配套清单/llm-task-registry.json。所有示例为人工fixture，不是模型实测输出。格式修复也属于生成调用，未获明确授权不得执行。

修改模板后记录hash/版本，新请求用新版，旧候选和历史Take仍使用当时记录。
