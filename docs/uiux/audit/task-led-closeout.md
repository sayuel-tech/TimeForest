# 任务导向 UIUX 公共复核（2026-09-12）

本文记录Q7的审查依据与边界；唯一当前状态仍见[维护基线](../../maintenance-baseline.md)，批次定义见[计划](../task-led-plan.md)。本批未修改运行代码，也未重新截取页面；各页视觉结论沿用对应批次已查看的最终画面，不表示本日重新完成全站视觉验收。

## 复核范围

以baseline-v6.3.34至当前工作树的实际改动为范围，核对已提交的Q1—Q4a与未提交Q4b—Q6b；不回退原首页/顶部导航，不搬动业务状态或生成默认。当前运行差异集中于21个前端文件。后六批最终after材料覆盖的16个运行文件与当前内容逐一一致（按文本规范化换行比较），未发现后续覆盖导致证据失效。

从页面卡和各批README选择31份最终checks.json，全部包含passed记录且通过；排除before、probe、失败夹具及被后续迭代替代的画面。这是对已有检查记录的核对，不将它们计作Q7重新执行。文件清单及当前SHA256见工作目录 `<本地维护路径>` 中的evidence-index.json与runtime-evidence-match.json。

## 公共动作矩阵

| 操作 | 当前含义与入口 | 证据与边界 |
|---|---|---|
| 查看候选、选用、入库 | candidateButton/resultActions负责共同呈现，业务回调分别执行；查看不写选用，入库保留候选选择，选择身份独立显示 | Q3a、Q5a、Q5b最终临时API证据；Q7结果与移除保护检查 |
| 选择资产/提示词 | 选择器返回固定版本，调用页通过原应用/保存门决定落盘；取消返回null | Q1、Q6a；未把库选择直接当作项目已保存或已生成 |
| 写作确认与继续编辑 | 原沟通/输出节点和事件保持，版本选择与阅读布局调整不重写保存机制 | Q2a/Q2b；上下文按内容对象隔离，保留来源/局部覆盖与离开保护 |
| 保存与迟到响应 | ProjectSession/ImageSession/原接续控制器各自持有状态，公共读戳与视图保护不替换正在编辑的草稿 | Q4c/Q5a既有场景；Q7保存失败、销毁、轮询与输入保护检查 |
| 续接与轨道排序 | 新续接使用生成依赖；track_order仅控制成片顺序，已选结果独立轨道槽位 | Q5b真实临时API与假导出快照；假文件时长不证明真实拼接输出 |
| 停止与恢复 | 停止请求只发给指定任务，仍等待引擎确认；恢复保留固定引用，不自动选用、生成或级联恢复子记录 | Q6b临时API/假引擎与任务、回收检查 |
| 目录与辅助侧栏 | 公共工作台保留各页业务布局，窄屏工具行只对实际冲突的shot-heading/image-tools留高度 | Q4c三视频与Q5a图片联查；不将局部规则扩为全站三栏 |

## 本批直接检查

执行5个现有Node测试文件，共36项通过：studio_frontend、experience_contract、studio_async_experience、studio_result_experience、studio_candidate_records。覆盖保存取消/失败、生成等待保存、迟到响应、图片轮询对象身份、输入/弹窗/播放保护、候选查看与入库分离、移除与未知状态保护，以及公共组件准入。输出保存在node-checks.txt。

UIUX索引、公共体验结构、context内容复核记录与差异空白检查另作维护收尾。结构工具仍报告五个历史模式的既有完善标记，本批不凭局部UIUX通过擅改为全量体验认可。

## 保留的未验收范围

- 剧本创作和电影创作仍按当前版本说明为不可用；布局与隔离业务片段通过不等于完整创作流程交付。后续应从真实代码的具体端到端缺口继续，不能因Q7收口解除声明。
- 真实模型、长片质量、真实引擎停止、生产进程是否加载当前源码及用户最终体验未在本轮验收。
- 七模式已有不同业务作用域，公共动作的含义保持一致，不强制所有页面具有相同列数、布局或容量。
- 本轮不包含生产重载、模型调用、Git提交、推送或发布。Q4b—Q6b未提交工作树及批次恢复材料保留，不能用HEAD整体覆盖。

复核未发现需要在Q7新增运行修改的公共行为回归，因此本批仅完成证据审查、直接公共检查和交接收口；不为结束批次制造额外改动。
