# 问题与待复核项

此处记录本轮盘点发现；后续实际完成状态以维护基线为准。没有记录问题不代表页面已获认可。

## F01 资产需求的主要任务被配置内容遮蔽（U/S/H，待设计）

用户明确要求先看出要绑定什么对象，使用具名图片位置。源码 binding-cards 同时呈现来源、上传、选择、关系与详情；历史 wide.png 也显示重复标题和较密卡片。应保留真实需求身份、分类、作用域和原绑定服务，调整主要信息与入口，不从后端重建业务。入口：[剧本](../pages/authoring.md)。

## F02 资产卡片区存在固定高度和后置覆盖（S，与 F01 一起处理）

static/studio/styles/creation.css 对 .creation-asset-stage .asset-need-groups 先设 max-height:460px，后又覆盖为320px；卡片 img 从150px被后置规则改为80px。源码可确认这种级联，历史截图也存在内部滚动。具体可用高度仍受实际视口和父容器影响；不能只删除一条 CSS 就宣称任务呈现问题已解决。

## F03 部分维护说明时间口径落后（S，本轮局部校准）

PROJECT_GUIDE 开头“当前只讨论、不改运行代码”与其后已实现纠偏入口冲突；设计规范 V2 中 design-tokens 尚为旧值的描述也与当前 reading/summary token 不一致。本轮纠正这两处。其他文档的四/五模式描述有些属于历史或特定调用方，不能批量替换为七；索引列出七个实际注册模式及能力开关。

## F04 共享工作区可访问名称过于具体（S/R，未改代码）

ui/workbench.js 的 rail 与 inspector 使用“片段目录”“本段属性”固定 aria-label，调用方包含图片任务等非片段场景。源码可确认命名，实际屏幕阅读器影响未验证。后续公共壳层改动时考虑由调用方提供准确名称并保留默认兼容，不能称当前所有键盘交互失效。

## F05 视觉 evidence 与运行版本不能互相替代（S/H）

现有截图为隔离 fixture 产物；本轮未检查生产网页是否加载当前文件。不要从截图日期或静态 6.3.32 标签推导当前服务内容。后续需要页面交互证据时使用对应临时 fixture，明确数据与服务边界。
