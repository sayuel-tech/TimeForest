# V3.0 隔离检查证据

[机器记录](verification.json)、[14 项新业务场景](backend.json)。真实 Flask API、原存储临时目录及真实 ES Modules；模型/媒体末端使用替身。四份截图来自临时项目，浏览器独立 profile 未纳入仓库。

静态配套归档清单范围、体积与SHA256见 [包校验](package.json)；12个新后端文件、10个新前端模块和资源JSON语法检查通过。归档逐项字节核对及ZIP完整性通过。

- [剧本宽屏]（本地检查图，未公开） / [电影宽屏]（本地检查图，未公开）
- [剧本窄屏]（本地检查图，未公开） / [电影窄屏]（本地检查图，未公开）

窄屏沿用原站整页滚动，检查滚动后底栏可达；宽屏检查主操作直接可见。测试曾把窄屏底栏错误要求为始终固定，按原公共响应式规则修正；未为测试新增模式专属底栏。来源返回检查发现原项目 UUID 被短 ID 校验拒绝，已修复并补 Node 与浏览器校验。

电影截图中的假媒体无法解码是隔离素材的边界，不是视频效果验收。连续预览只验入口、原播放器与关闭生命周期；真实连续播放、生成质量与接点声画由用户后续实测。没有以截图替代数据绑定检查，也没有把这些有限场景写成全部交互验收。

复查入口：`tests/creation_checks.py`、`tests/creation_ui_fixture.py`、`tests/creation_return_context.test.mjs`。原 56 项回归另补 7 项素材包检查，共 63 项通过；Node 共 35 项通过。艺术资源未完成，体验准入预期留下 authoring/visual 与 movie/visual 两项失败。
