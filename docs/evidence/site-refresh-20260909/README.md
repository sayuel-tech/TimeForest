# 全站更新与库界面检查证据

- `workspace-checks.json`：7个实际模式控制器。项目与目录从真实临时API读取；仅项目轮询响应中的进度、revision和名称被受控修改。检查相同轮询DOM身份、进度局部更新、内容变化/展开保护、输入中迟到响应；另验证按稳定身份重排任务行保持展开/焦点。不是实际生成。
- `library-checks.json`：资产/提示词/更多导入弹窗在1920与760请求窗口共6场景。实际内容窗口分别1902与742px；真实临时库12项资产、模型分支和提示词。检查160px缩略区、宽屏列数、当前用途/模型、全库搜索切换、条目选中、导入取消保留多选、横向边界。截图使用原站已有入口插画作临时素材，非生产资产。
- `prompt-save-checks.json`：两种宽度下原5模式工具栏、取消、应用、真实保存收录、空分支管理、固定版本/字段组合/不支持字段及收藏去重均通过。
- 相关Node检查28项通过，覆盖快照分类、延后内容不被运行合入吞掉、读归属/草稿保护、任务卡及新增体验准入；114个前端JS文件语法检查通过。体验接入结构检查通过，保留5个历史模式的整体体验待完善登记，不将本轮定向结果扩充为全部功能认证。
- 已查看宽屏资产、提示词、导入弹窗和窄屏代表截图。初次弹窗截图截到进入动画，最终截图等待动画结束后重新采集；不将动画中透明帧误判为样式结果。

复跑入口：`tests/site_refresh_ui_fixture.py <目录>`、`tests/library_layout_ui_fixture.py <目录>`、`tests/prompt_library_ui_fixture.py --evidence-dir <目录>`。使用本机现有Python/Chrome，不安装环境。creation fixture保留禁止Python外部socket的原防护，用子进程浏览器读取临时站点；没有解除网络保护以做模型测试。

未运行真实生成、大文件/大型库/长时多窗口测试；未生产重载、提交或发布。完整本地检查产物位于`<本地维护路径>`，这里只存必要回执和代表截图。
