# 信息密度、原版字体与动效修复

## 用户要求与当前范围

2026-09-10用户要求修复模块过大、同屏信息量少，以及字体和原有动效观感变化。保留已恢复的原版首页、顶部全站导航，其他工作区仍按现有业务位置组织。工作目录为<本地维护路径><仓库根目录>；唯一当前状态见[维护基线](../maintenance-baseline.md)。

## 诊断证据

通过原站静态样式的隔离浏览器探针，以及现有Edge新旧页面的只读字体检查，确认正文实际加载的均为Onest／Noto Sans SC，标题为EditorialSerif／Noto Serif SC；208个字体资源引用均存在，字体文件没有丢失或被替换。差异在R1项目标题从原版500字重改为600，及项目卡片的原阴影／悬停被较弱覆盖。已恢复500字重并去除该覆盖，没有另换字体来掩盖问题。

用户当时打开的首页仍是uiux-1。完成修复后确认该首页没有可见编辑输入或打开的对话框，再刷新首页，实查uiux-3、顶部导航、原版hero、500字重、panel-in入场和0.22s卡片过渡已加载。另一个旧剧本页有未保存内容，没有刷新、保存或修改它。

尺寸问题用同一长文、三个资产需求、1366×768窗口复现：修复前首屏完整正文行数为0。经过实际布局调整，最终同一场景可见6行，资产卡由原R1约207px降到约141px。中间0行／5行失败记录保留；没有降低六行断言来通过检查。该数值针对明确的临时内容与窗口，不承诺任意数量素材均有同样首屏容量。

## 代码归属与改变

| 内容 | 所属入口 | 本次改变 |
|---|---|---|
| 字体及尺度 | styles/design-tokens.css、shell.css、components.css | 原字体家族／字体文件保留；标题恢复500；正文15px、阅读16px／1.7行高；控件32px、次级28px、目录188px、参考248px，公共页头／步骤／底栏压缩。触屏命中区保留44px。 |
| 资产与正文 | features/authoring-assist/binding-cards.js、ui/experience-content.js、styles/creation.css | 需求标题／计数／筛选／帮助／动作共用工具行；描述移入更多而非丢弃；名称／状态／固定来源仍可见。重复沟通标题并入上下文，版本操作并入原输出summary，正文和原事件节点保持。 |
| 目录 | features/shot-segment-tree/index.js、workbench.css | 名称单行省略，完整名称仍在title及主内容中；身份、层级、选中和确认状态不改。 |
| 播放与剪辑 | ui/media-player.js、media-player.css、creation.css | 原生视频控件保留；额外精确定位滑块归到工具栏折叠项，减少每个视频下方重复控制条。电影所选片段标题和移动／纳入动作同行，1366首屏入出点可达。 |
| 两库与各模式 | collection-navigation.css、library.css、task-layouts.css | 减少筛选、字段、导航和卡片内边距；图库缩略高度128px；清理520px审核区及过大的短输入最小高度。长文和原图仍可完整查看，画布不做缩放伪装。 |
| 原动效 | shell.css、components.css、base.css | 恢复项目卡原-3px悬停／阴影，保留按钮按压、首页入场、弹窗动效；减少动态效果偏好和隐藏页暂停机制继续有效。 |

上表styles及前端路径相对static/studio。原会话草稿、revision、固定资产版本、候选、原保存门、媒体像素和模型参数不变。阅读区尺寸变化不截断内容；说明、精确定位和详细需求仍可展开操作。新字号不是再定义一套字体主题。

## 必要检查与局限

- visual_style_fixture：复现600字重失败，修复后500通过；浏览器实际字体审计、正常动效与减少动态效果两种状态通过。原型截图或字体文件哈希不替代实际字体检查。
- creation_experience_ui_fixture --density：真实临时API走五页写作、固定响应、资产绑定／确认／保存／筛选，再测长文首屏容量；1366×768至少6行通过。无真实LLM调用。
- site_layout_ui_fixture：七模式23步骤在1366×768、7个代表页在430×900的边界与底栏检查通过。
- library_layout_ui_fixture：首页、资产、提示词和导入取消在1366／430共8例通过，保留多选。
- creation_ui_fixture：真实临时电影参数／候选／剪辑保存、750ms主播放器取点、非法时间码失败留存及源项目返回通过；使用本地CPU测试片，无真实模型。首屏入出点断言覆盖1366×768。
- uiux_image_canvas_fixture：430宽度真实指针遮罩、撤销／重做、遮罩持久化、96px扩边保存及跨任务隔离通过，0次引擎调用。
- 25项相关Node回归通过；字体／动效、UIUX索引、体验准入、视觉尺寸及上下文检查分别记录，不累计重复运行。

证据与修改前副本：<本地维护路径>。字体／动效探针是静态页面，业务检查用临时库。真实生产仅对无编辑的首页进行前端刷新和只读核对；未重启生产进程、未访问生产凭据、未调用模型或发布。最终日常体验继续由用户反馈，不把本批密度改善等同于全站体验认证或解除剧本／电影不可用声明。

## 回退

对照本批before及逐文件差异恢复需要撤回的源码，保留后续编辑；UIUX机械图及context回执按原流程重新复核生成。新增测试为复现与回归入口，不属于生产页面。本批材料在工作目录保留用于证据与恢复；工作目录整体仍承担任务组织、方案、素材和交付，代码维护在真实仓库。

## 接手续尾复核

2026-09-10接手复核发现context回执仍停在uiux-2，而源码和人工交接已更新uiux-3。已核对density-final、layout-wide/narrow、library、movie-final、canvas和motion-normal/reduced的最终检查JSON；过程失败记录继续保留，不当作最终未修复。此次未重新执行这些浏览器场景，另在当前源码运行15项公共视觉规范/体验准入Node检查通过。修正剧本页面卡和项目地图的旧待办表述，保持不可用声明；最新收尾状态以维护基线及实际context检查为准。
